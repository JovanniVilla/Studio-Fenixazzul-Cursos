//strings
//cambiar valores de las varibles

const nombre = 'Fernado'
const apellido = 'Herrera'

//considera como codigo JS los que esta dentro de las llaves 
// Ejemoplo ${" esto es lo que se leeria como codigo"}

const nombreCompleto = `${nombre} ${apellido}`

console.log(nombreCompleto)